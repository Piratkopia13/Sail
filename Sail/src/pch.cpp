#include "pch.h"

//void* operator new(size_t size, char* file, int line, char* function) {
//	std::cout << "Allocated " << size << " of something in file " << file << '\n';
//	return malloc(size);
//}
